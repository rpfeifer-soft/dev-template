version: "3.1"

services:

  server:
    container_name: server
    build: 
      context: .
      dockerfile: ./server/dev.Dockerfile
    ports:
      - "9229:9229"
    environment:
      - SECRETS=/app/options.json
    expose: 
      - "3001"
      - "8080"
    volumes:
      - ./build:/app/build
      - ./package.json:/app/package.json
      - ./nodemon.json:/app/nodemon.json
      - ./server/secrets.json:/app/options.json
  
  nginx:
    container_name: nginx
    image: nginx
    ports:
      - "80:80"
    depends_on:
      - server
    volumes:
      - ./server/dev.nginx.conf:/etc/nginx/nginx.conf:ro
